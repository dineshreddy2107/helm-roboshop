apiVersion: v1 
kind: Service 
metadata:
  name: mysql 
  namespace: roboshop
  labels:
     component: mysql
     project: roboshop
     tier: database
spec:
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
  
  selector:
     component: mysql
     project: roboshop
     tier: database